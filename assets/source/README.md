# Source Assets

AI 생성물은 검수와 정규화가 끝나면 후보로 남기지 않고 최종 런타임 에셋으로 승격한다.

최종 PNG는 역할에 맞게 `assets/backgrounds/`, `assets/sprites/`, `assets/tiles/`, `assets/ui/` 아래에 둔다. 승격 후 중복 원본과 중간 처리 이미지는 `assets/source/`에 보관하지 않는다.
