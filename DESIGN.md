# 디자인

## 정본

- 상태: Active
- 갱신일: 2026-09-03
- 제품 화면: Godot 4.x 모바일·데스크톱 게임 HUD, 메뉴, 대화 화면
- 검토 근거: `AGENTS.md`, `docs/architecture.md`, `assets/style/art-style-tokens.json`, `src/ui/game_hud.gd`, `src/ui/pixel_ui_theme.gd`
- 상위 정본: Notion `09. 아트디렉션·맵·UI`와 `🎨 아트 에셋` DB

## 브랜드

- 귀여운 여우 사무라이, 다도적 고요, 와비사비의 사용감을 유지한다.
- 선명한 실루엣과 절제된 정보 표현으로 신뢰를 만든다.
- 고광택 모바일 RPG, 네온, 과도한 장식과 복잡한 계기판 표현을 피한다.

## 제품 목표

- 전투과 탐색 중 핵심 상태를 짧은 시선 이동으로 파악하게 한다.
- 모바일에서도 맵을 중심에 두고 가장자리 HUD를 쉽게 누르고 읽게 한다.
- 장식보다 플레이 판단에 필요한 상태 변화를 우선한다.
- 비목표: UI가 게임 규칙이나 상태를 소유하지 않는다.

## 사용자와 할 일

- 주 사용자: 모바일 또는 데스크톱에서 짧은 로그라이크 런을 플레이하는 사용자다.
- 사용자는 전투 중 `HP / 기운 / 心`, 시간대, 즉시 사용 가능한 행동을 빠르게 확인해야 한다.
- 터치 환경에서는 작은 글자나 정밀 드래그 없이 주요 명령을 실행해야 한다.

## 정보 구조

- 화면 중앙: 월드와 전투 상황
- 상단 가장자리: 플레이어 자원, 바이옴, 시간대
- 하단 가장자리: 이동과 주요 행동
- 일시 메뉴: 인벤토리, 제작, 시설, 차 우리기, 도감, 지도
- 대화 오버레이: 화자, 초상, 선택지

## 디자인 원칙

- 읽기 쉬운 픽셀 실루엣을 세부 묘사보다 우선한다.
- 상시 자원은 `HP / 기운 / 心`만 사용한다.
- 시간은 자원 게이지가 아니라 낮·밤과 수면 판단을 돕는 환경 상태로 표현한다.
- 동일한 상태는 아이콘, 색, 짧은 텍스트 중 둘 이상으로 구분한다.
- 반복되는 HUD 요소는 기존 컴포넌트와 토큰을 재사용한다.

## 시각 언어

- 색상과 이미지 제작 방향은 `assets/style/art-style-tokens.json`만 정본으로 사용한다.
- 글꼴은 `Galmuri11`과 nearest filtering을 사용한다.
- 패널은 짙은 먹색 바탕, 흙빛 테두리, 작은 여백을 유지한다.
- 시간 시계는 12개 분절 원호와 중앙 해·달 표식을 사용한다. 낮은 기운 황금 계열, 해질녘은 여우 적갈색 계열, 밤은 흐린 강물·밤 남색 계열로 구분한다.
- 움직임은 상태 전달에 필요할 때만 짧게 사용하며 지속적인 장식 애니메이션은 피한다.

## 컴포넌트

- `PixelUiTheme`: 공통 글꼴, 버튼, 패널 스타일을 소유한다.
- `GameHud`: 런타임 read model을 관찰하고 HUD와 명령 표면을 렌더링한다.
- 시간 시계: 시간대 아이콘, 12분절 진행 원호, 시간대 이름, 진행률을 함께 표시한다. 게임 시간 계산은 소유하지 않는다.
- 자원 아이콘 행: `HP / 기운 / 心`의 현재 비율을 작은 반복 아이콘으로 표시한다.

## 접근성

- 색상만으로 상태를 구분하지 않고 아이콘과 텍스트를 함께 제공한다.
- 키보드 포커스와 터치 입력을 모두 지원한다.
- 작은 원형 지표에도 시간대 이름과 숫자 진행률을 남긴다.
- 번쩍임과 빠른 반복 애니메이션을 사용하지 않는다.

## 반응형 동작

- HUD는 안전 영역을 반영하고 화면 가장자리에 배치한다.
- 모바일 버튼은 충분한 터치 영역을 확보한다.
- 좁은 화면에서는 월드 가시성을 위해 HUD 정보를 짧게 유지한다.
- 데스크톱과 모바일은 같은 command 계층을 사용하며 표현과 입력 방식만 달라질 수 있다.

## 상호작용 상태

- 비활성 행동은 버튼 상태와 낮은 대비로 표시한다.
- 빈 목록은 명시적인 빈 상태 문구를 표시한다.
- 성공과 실패 피드백은 짧은 문구와 기존 피드백 음향을 사용한다.
- 메뉴 갱신은 입력 신호 방출 중인 Control을 즉시 해제하지 않는다.

## 콘텐츠 문체

- 한국어를 기본으로 사용한다.
- 짧고 구체적인 명사형 또는 행동형 문구를 쓴다.
- `HP / 기운 / 心`과 정본의 고유 명칭을 임의로 바꾸지 않는다.

## 구현 제약

- UI는 domain state를 소유하지 않고 read model을 렌더링하며 command만 보낸다.
- 32x32 기반 저해상도 픽셀아트와 정수 배율, nearest filtering을 전제로 한다.
- 새 색상 팔레트나 이미지 생성 토큰을 코드나 문서에 별도 정본으로 만들지 않는다.
- 변경 후 관련 unit test와 Godot headless 검증을 실행한다.

## 열린 질문

- [ ] 실제 기기에서 28px 시간 시계의 최소 가독성을 확인한다. 담당: UI 검수. 영향: 필요하면 크기와 글자 간격만 조정한다.
